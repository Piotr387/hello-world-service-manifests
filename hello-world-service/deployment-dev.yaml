name: hello-world-service-container
image: ""
ports:
  - containerPort: 8080
env:
  - name: APP_NAME
    value: "HelloWorldService-${POD_NAME}"
  - name: POD_NAME
    valueFrom:
      fieldRef:
        fieldPath: metadata.name
